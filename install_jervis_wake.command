#!/bin/zsh
# Double-click to install Jervis Wake: "Hey Jervis" opens Jervis even when Jervis and VS Code are closed.
# (The work is done by wake/install.sh; see the comments there.)
cd "$(dirname "$0")" && exec bash wake/install.sh
